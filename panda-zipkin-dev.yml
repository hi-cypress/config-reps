zipkin:   
  collector:
    rabbitmq:
      addresses:
      port:
      username:
      password:
      virtual-host: /
      queue: zipkin
  storage:
    type: mysql
    StorageComponent: mysql

#关闭验证
management:
  metrics:
    web:
      server:
        auto-time-requests: false
